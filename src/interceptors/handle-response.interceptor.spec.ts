import { HandleResponseInterceptor } from './handle-response.interceptor';

describe('HandleResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new HandleResponseInterceptor()).toBeDefined();
  });
});
